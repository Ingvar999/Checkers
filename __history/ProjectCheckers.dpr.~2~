program ProjectCheckers;

uses
  Forms,
  MenuUnit in 'MenuUnit.pas' {Menu},
  BoardCreateUnit in 'BoardCreateUnit.pas' {Checkers};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TMenu, Menu);
  Application.CreateForm(TCheckers, Checkers);
  Application.Run;
end.
